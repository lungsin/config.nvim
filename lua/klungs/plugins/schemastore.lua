-- JSON/YAML schemas from SchemaStore.org
-- Used by the yamlls / jsonls language servers (see lsp/yamlls.lua)
return {
  'b0o/SchemaStore.nvim',
  lazy = true,
  version = false, -- last release is way too old
}
